<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:xf="clr-namespace:Prism.Navigation.Xaml;assembly=Prism.Forms"
             xmlns:viewModels="clr-namespace:PrismApp.ViewModels"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="clr-namespace:Prism.Navigation.Xaml;assembly=Prism.Forms"
             x:Class="PrismApp.Views.MainPage"
             mc:Ignorable="d"
             Title="{Binding Title}">

    <StackLayout>
        <Entry x:Name="input" d:Text="input" Text="{Binding Input, Mode=TwoWay}"/>
        <Button Text="ToUpper" Command="{Binding ToUpperCommand}" />
        <Label d:Text="INPUT" Text="{Binding Output}" />
        <Button Text="Go to Next"
                Command="{prism:NavigateTo NextPage}" 
                CommandParameter="{Binding Text, Source={Reference input}}"/>
    </StackLayout>

</ContentPage>